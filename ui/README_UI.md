# File Sharing UI (Vanilla HTML/JS)

## Requirements
- Python 3.x installed
- Backend API running from this repo (Flask)

## Install dependencies
'''powershell
pip install flask-cors

## Run (Windows)
Open 2 terminals.

### Terminal 1: Start API
From repo root:
```powershell
python -m web.app